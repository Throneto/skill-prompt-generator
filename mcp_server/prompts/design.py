#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Design Prompt Generation SOP
"""


def generate_design_prompt_sop(description: str) -> str:
    """
    Generate a SOP prompt for design prompt generation.
    
    Args:
        description: User's design description
    
    Returns:
        SOP instruction for AI
    """
    return f"""
你是专业的AI图像提示词工程师，专注于平面设计领域。请按照以下SOP生成设计类提示词。

## 用户需求
{description}

## 执行步骤

### 第一步：解析意图
调用 `parse_user_intent` 工具：
- user_request: "{description}"
- domain: "design"

识别：
- 设计类型（海报、UI、Bento Grid等）
- 风格（玻璃态、极简、渐变等）
- 用途（商业、社交媒体、品牌等）

### 第二步：查询元素
调用 `query_elements` 查询 design 领域的元素：
- domain: "design"
- category: 根据设计类型选择

常用类别：
- "layout_styles" (布局风格)
- "visual_effects" (视觉效果)
- "color_schemes" (配色方案)
- "typography" (字体排版)

### 第三步：选择元素
从候选中选择最匹配的元素，优先考虑：
- 现代设计趋势
- 视觉层次和信息架构
- 品牌一致性

### 第四步：生成提示词
调用 `compose_prompt` 组合元素。

### 第五步：输出结果

```
🎨 设计解析
- 类型：[设计类型]
- 风格：[设计风格]
- 用途：[使用场景]

✨ 最终提示词
────────────────────────────────────────
[提示词内容]
────────────────────────────────────────
```
"""


def generate_bento_grid_sop(description: str) -> str:
    """
    Specialized SOP for Bento Grid design.
    """
    return f"""
你是Bento Grid设计专家。请生成专业的Bento Grid布局AI提示词。

## 用户需求
{description}

## Bento Grid 设计要点
- 网格系统：不规则但有序的模块化布局
- 视觉层次：大小对比、颜色权重、留白
- 现代风格：圆角、微妙阴影、渐变
- 信息密度：适度的内容分区

## 执行步骤

1. 调用 `parse_user_intent` 解析需求
2. 调用 `query_elements` 查询 design 领域相关元素
3. 确保包含布局、配色、效果等元素
4. 调用 `compose_prompt` 生成提示词

## 输出格式

```
📐 Bento Grid 提示词
────────────────────────────────────────
[提示词内容]
────────────────────────────────────────
```
"""


def generate_glassmorphism_sop(description: str) -> str:
    """
    Specialized SOP for Glassmorphism design.
    """
    return f"""
你是玻璃态(Glassmorphism)设计专家。请生成专业的玻璃态UI设计AI提示词。

## 用户需求
{description}

## Glassmorphism 设计要点
- 透明度：frosted glass effect, 50-80% opacity
- 模糊：background blur, gaussian blur
- 边框：subtle white border, 1px transparency
- 阴影：soft shadow, layered depth
- 背景：gradient, vibrant colors

## 执行步骤

1. 调用 `parse_user_intent` 解析需求
2. 调用 `query_elements` 查询相关元素
3. 调用 `compose_prompt` 生成提示词

## 输出格式

```
🔮 Glassmorphism 提示词
────────────────────────────────────────
[提示词内容]
────────────────────────────────────────
```
"""
